apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: crt-secret
  namespace: monitoring  
spec:
  secretName: crt-secret-v2 
  
  issuerRef:
    name: cluster-issuer  
    kind: ClusterIssuer

  dnsNames:
    - grafana.daanggo.com