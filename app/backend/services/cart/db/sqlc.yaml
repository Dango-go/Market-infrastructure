version: "2"
sql:
  - engine: "postgresql"
    schema: "migrations"
    queries: "queries"
    gen:
      go:
        package: "cartdb"
        out: "../internal/repository/postgres/gen"
        sql_package: "pgx/v5"
        emit_interface: true
        emit_pointers_for_null_types: true
